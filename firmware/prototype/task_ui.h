// UI 태스크 — core 0, 앱 태스크보다 낮은 우선순위.
//
// 지금 하는 일: 5방향 스위치를 KEY_POLL_MS 마다 읽어 디바운스하고, 눌린 순간마다
// Command{CMD_BUTTON, SRC_KEY} 를 q_cmd 에 넣는다. 5단계에서 LCD 그리기가 이 루프에 붙는다
// (버튼 읽기 → LCD 그리기를 차례로 — LCD 에 쓰는 동안에는 버튼을 읽지 않는다).
//
// --- 지켜야 할 것 ---
//  1. 모터·상태 모델·시리얼을 건드리지 않는다. 판단은 app_task 가 q_cmd 에서 꺼내 한다(목표 7.2~7.3).
//  2. 기다리지 않는다 — delay() 도, 떼기를 기다리는 while 도 없다(목표 5.5). 큐가 가득 차면 버린다.
//  3. 이 태스크가 멈춰도 모터 판단과 호흡 검출은 계속 돈다(우선순위가 더 낮고 입출력만 한다).

#ifndef TASK_UI_H
#define TASK_UI_H

// 버튼 핀을 설정하고 UI 태스크를 core 0 에 만든다. q_cmd 가 만들어진 뒤에 부를 것.
void ui_start();

#endif  // TASK_UI_H
